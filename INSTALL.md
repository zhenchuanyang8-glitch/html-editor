# HTML可视化编辑器 - 安装和使用指南

## 快速开始

### 1. 安装扩展

1. 打开Chrome浏览器，访问 `chrome://extensions/`
2. 开启右上角的"开发者模式"开关
3. 点击"加载已解压的扩展程序"按钮
4. 选择 `html-editor-extension` 文件夹
5. 扩展安装成功！浏览器工具栏会出现编辑器图标

### 2. 生成图标（可选）

由于安全限制，扩展需要PNG图标文件。请执行以下步骤：

1. 在浏览器中打开 `generate-icons.html`
2. 点击"生成图标"按钮
3. 下载生成的3个图标文件
4. 将图标文件放入 `icons` 文件夹
5. 在 `chrome://extensions/` 中点击扩展的"刷新"按钮

或者直接使用SVG图标（已包含）：编辑 `manifest.json`，将图标引用改为 `.svg` 文件（已配置）。

### 3. 使用编辑器

1. 在文件管理器中双击打开本地HTML文件
2. 点击浏览器工具栏中的HTML编辑器图标
3. 在弹窗中点击"启动编辑器"按钮
4. 页面顶部会出现工具栏
5. 开始编辑：
   - **文字编辑**：直接点击页面中的文字进行编辑
   - **格式化**：选中文字后点击工具栏按钮
   - **表格编辑**：点击表格单元格，使用表格操作按钮
   - **插入元素**：使用工具栏的插入按钮
   - **连接线**：点击连接线按钮，然后依次点击两个元素
6. 编辑完成后点击"保存文档"按钮

## 功能说明

### 文本编辑
- **基础格式**：加粗、斜体、下划线、删除线
- **字体设置**：大小、颜色、背景色
- **段落格式**：标题、列表

### 表格功能
- **编辑内容**：直接点击单元格编辑
- **调整大小**：拖动表格边框
- **增删行列**：使用工具栏按钮
- **更改颜色**：选中单元格后设置颜色

### 形状工具
- **矩形**：插入矩形或圆角矩形
- **圆形**：插入正圆或椭圆
- **箭头**：插入带箭头的线条
- **特殊形状**：三角形、星形、六边形等

### 连接线
1. 点击"连接线"按钮进入连接模式
2. 点击第一个元素（起始点）
3. 点击第二个元素（结束点）
4. 连接线自动创建

### 保存文档
- Chrome 86+：使用File System Access API直接保存
- 其他浏览器：下载编辑后的HTML文件

## 注意事项

1. **仅支持本地HTML文件**：扩展只能在 `file://` 协议下工作
2. **浏览器兼容性**：需要Chrome 86或更高版本
3. **文件权限**：首次保存需要授权文件访问权限
4. **备份数据**：建议在编辑前备份原始文件

## 测试

使用项目中的 `test.html` 文件测试各项功能：

1. 双击打开 `test.html`
2. 启动编辑器
3. 尝试各种编辑功能
4. 保存并验证结果

## 故障排除

### 扩展图标不显示
- 刷新扩展：在 `chrome://extensions/` 中点击刷新按钮
- 检查图标文件：确保 `icons` 文件夹中有图标文件

### 编辑器无法启动
- 确认打开的是本地HTML文件（不是网页）
- 检查浏览器控制台是否有错误信息
- 重新加载扩展

### 表格无法调整大小
- 确保已启用编辑模式
- 刷新页面重新启动编辑器
- 检查是否有JavaScript错误

### 保存功能不工作
- 确认浏览器版本支持File System Access API
- 尝试使用传统下载方式
- 检查文件权限设置

## 卸载

1. 访问 `chrome://extensions/`
2. 找到"HTML可视化编辑器"
3. 点击"移除"按钮

## 技术支持

如有问题或建议，请通过以下方式联系：

- GitHub Issues（如果有仓库）
- 电子邮件（如果有联系方式）

## 更新日志

### v1.0.0 (2025-01-15)
- 初始版本发布
- 基础文本编辑功能
- 表格编辑功能
- 形状绘制功能
- 连接线功能
- 本地文件保存功能
